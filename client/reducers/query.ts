import { QUERY } from './type'

export default function queryReducer (state, action) {
    switch(action.type){
        case QUERY:
            break;
        case 1:
            break;
        case 2:
            break;
        default:break;
    }
}