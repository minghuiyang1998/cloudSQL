import { SCHEMA } from './type'

export default function schemaReducer (state, action) {
    switch(action.type){
        case 1:
            break;
        case 2:
            break;
        case 3:
            break;
        default:break;
    }
}