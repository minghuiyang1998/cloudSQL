import { SCHEMA } from './type'

export default function connectionReducer (state, action) {
    switch(action.type){
        case 1:
            break;
        case SCHEMA:
            break;
        case 3:
            break;
        default:break;
    }
}